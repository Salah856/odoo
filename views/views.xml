<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="hospitalms.list">
      <field name="name">hospitalms list</field>
      <field name="model">hospitalms.hospitalms</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="hospitalms.action_window">
      <field name="name">hospitalms window</field>
      <field name="res_model">hospitalms.hospitalms</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="hospitalms.action_server">
      <field name="name">hospitalms server</field>
      <field name="model_id" ref="model_hospitalms_hospitalms"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="hospitalms" id="hospitalms.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="hospitalms.menu_1" parent="hospitalms.menu_root"/>
    <menuitem name="Menu 2" id="hospitalms.menu_2" parent="hospitalms.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="hospitalms.menu_1_list" parent="hospitalms.menu_1"
              action="hospitalms.action_window"/>
    <menuitem name="Server to list" id="hospitalms" parent="hospitalms.menu_2"
              action="hospitalms.action_server"/>
    -->
  </data>
</odoo>